spring:
  application:
#    应用实例名，同时也对应dataId的${prefix}
    name: order-web
  cloud:

#########################################################################################################################
############################################# Apollo Configuration Begin ################################################
#app:
##  应用ID
#  id: platform
#apollo:
##  Meta Server地址，在集群环境下为多个，生产环境还是建议使用域名（走SLB），因为机器扩容、缩容等都可能导致IP列表的变化
#  meta: http://10.1.61.101:7546
#  bootstrap:
##    是否在bootstrap阶段向Spring容器注入被托管的文件的配置信息
#    enabled: true
##    命名空间，若不为properties格式时则需要带上扩展名
#    namespaces: common
############################################# Apollo Configuration End ##################################################
#########################################################################################################################